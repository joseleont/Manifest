<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clase3_4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_icoo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_icoo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".retornp"></activity>
        <activity android:name=".actiINFO"></activity>
        <activity
            android:name=".actiURL"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".apps.Usuario" />
        <activity android:name=".apps.Registro" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>